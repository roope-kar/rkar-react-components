import styled from 'styled-components';
import type { MenuItemProps } from 'src/components/Menu/Menu.d';

const MenuItem = styled.div<MenuItemProps>`
  padding-top: 2px;
  padding-bottom: 2px;
`;

export default MenuItem;
