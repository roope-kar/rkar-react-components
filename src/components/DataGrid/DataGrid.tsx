import React from 'react';
import type { DataGridProps } from 'src/components/DataGrid/DataGrid.d';
import { GroupComponent } from 'src/types';

const DataGrid: GroupComponent<DataGridProps> = () => {
  return (
    <div></div>
  )
};

export default DataGrid;
