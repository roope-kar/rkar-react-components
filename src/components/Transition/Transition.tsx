import React from 'react';
import { TransitionProps } from 'src/components/Transition/Transition.d';

const Transition: React.FunctionComponent<TransitionProps> = (props: TransitionProps) => {
  return null;
};

export default Transition;
